## Prompt for Cursor Agent

> Build a founding-user waitlist signup for our coming-soon landing page.
>
> **Goal:** Capture email (required) and optional name/username. Persist signups via existing server action or API route pattern in this repo.
>
> **Requirements:**
> - Integrate into current landing page CTA section; match existing visual language
> - Client form with loading, error, and success states
> - Accessible labels, `focus-visible` styles, semantic HTML, unique `id` on every interactive element
> - Validate email format client-side; show clear error messages
> - Do not add complex domain schemas — only waitlist contact fields
>
> **Verify:** Submit works, success state shows, mobile (375px) and desktop layouts, keyboard navigation.
>
> **Out of scope:** Email provider integration, auth, admin dashboard, marketing automation.

---

**Target:** Cursor / IDE coding agent  
**Task type:** build  
**Assumptions:** Next.js App Router repo with existing landing page and waitlist backend stub  
**Tips:** @-mention landing page component and any existing form/server-action patterns before sending.
